# Core ML
tensorflow>=2.15.0
numpy>=1.24.0

# Data validation
pydantic>=2.0.0

# Image processing (color_extractor.py)
opencv-python>=4.8.0
scikit-learn>=1.3.0

# Weather API
requests>=2.31.0
python-dotenv>=1.0.0

# Flask backend
flask>=3.0.0
flask-sqlalchemy>=3.1.0
flask-migrate>=4.0.0
flask-jwt-extended>=4.6.0
flask-bcrypt>=1.0.0
flask-cors>=4.0.0
flask-limiter>=3.5.0
pillow>=10.0.0

# PostgreSQL driver (for Supabase)
psycopg2-binary>=2.9.0

# MySQL driver — local dev fallback
PyMySQL>=1.1.0
cryptography>=41.0.0

# Response compression
flask-compress>=1.14

# Production WSGI server
gunicorn>=21.2.0

# Testing
pytest>=7.4.0

# AI Generative Try-On (VTO) — gradio_client calls remote HF Spaces
gradio_client>=1.0.1

# CLIP sub-category tagging (optional — degrades gracefully if absent)
# Install locally for full feature set:
#   pip install torch torchvision transformers accelerate safetensors
# These are excluded from production to keep the Docker image under 2GB RAM.
